# 🚀 راهنمای کامل انتقال ربات تریاکی از Railway به سرور Ubuntu VPS
### + مهاجرت دیتابیس از SQLite به PostgreSQL روی همان سرور

این راهنما برای کسی نوشته شده که **هیچ‌وقت با لینوکس و سرور کار نکرده**. هر دستور دقیقاً همون‌جور که هست کپی می‌کنی و توی ترمینال صفحه‌کلیدت پیست می‌کنی و Enter می‌زنی. بعد از هر دستور مهم هم توضیح دادیم که چیکار کرد و چرا لازم بود.

**چیزی که لازم داری قبل از شروع:**
- یه سرور VPS تازه با **Ubuntu 24.04 LTS** (از هر ارائه‌دهنده‌ای) — آدرس IP سرور و رمز root‌ش رو بهت دادن
- توکن ربات از BotFather (همونی که تو Railway به‌کار می‌رفت)
- تلگرام‌آیدی عددی ادمین‌هات (همون مقادیر `TERIAKY_ADMIN_IDS` فعلی)
- فایل ZIP پروژه (همین پکیجی که این فایل داخلشه)
- فایل بک‌آپ دیتابیس که با دستور `/backup` از ربات می‌گیری (مرحله‌ی ۴ میگیم چطور)

> 📝 **موافقت نام‌های این راهنما:** همه‌جا اسم کاربر سرور رو `teriaky`، اسم دیتابیس رو `teriaky` و IP سرور رو `SERVER_IP` فرض کردیم. رمز دیتابیس رو هم `RAMZ-QAVI-123` گذاشکارتل — **حتماً با رمز خودت عوضش کن**.
>
> ⚠️ رمز دیتابیس رو فقط از **حروف انگلیسی و عدد** انتخاب کن (بدون `@` و `#` و `:` و فاصله) چون این کاراکترها توی آدرس اتصال دیتابیس خرابکاری می‌کنن.

---

## بخش ۱ — آماده‌سازی اولیه‌ی سرور Ubuntu

### ۱.۱ اتصال اولیه با SSH از ویندوز

ویندوز ۱۰ و ۱۱ به‌صورت پیش‌فرض برنامه `ssh` رو دارن؛ **هیچ برنامه‌ای (مثل PuTTY) لازم نیست نصب کنی**.

برنامه **PowerShell** رو باز کن (دکمه Start ← تایپ کن `PowerShell` ← Enter) و این دستور رو بزن:

```powershell
ssh root@SERVER_IP
```

به‌جای `SERVER_IP` آدرس IP سرورت رو بذار (مثلاً `ssh root@185.220.101.5`). این دستور یه پنجره‌ی امن به ترمینال سرورت باز می‌کنه.

- بار اول این سوال رو می‌پرسه: `Are you sure you want to continue connecting (yes/no/[fingerprint])?` ← تایپ کن `yes` و Enter بزن. این یعنی به اثرانگشت سرور اعتماد می‌کنی.
- بعد رمز root سرور رو می‌خواد. موقع تایپ رمز **هیچی روی صفحه نمیاد، حتی ستاره** — این طبیعیه! با خیال راحت تایپ کن و Enter بزن.

✅ اگه جمله‌ای شبیه `root@ubuntu:~#` دیدی یعنی وصل شدی. از اینجا به بعد همه‌ی دستورها توی همین پنجره زده میشن، مگر اینکه گفته باشیم برگرد به PowerShell ویندوز.

> ⚠️ **اگه این خطا رو دیدی:** `'ssh' is not recognized` ← نسخه‌ی ویندوزت OpenSSH رو فعال نکرده. برو به Settings ← Apps ← Optional Features ← Add a feature و **OpenSSH Client** رو نصب کن و PowerShell رو ببند و دوباره باز کن.

### ۱.۲ به‌روزرسانی سیستم

```bash
apt update && apt upgrade -y
```

این دستور لیست بسته‌های نرم‌افزاری سرور رو تازه می‌کنه (`update`) و همه‌ی بسته‌ها رو به آخرین نسخه ارتقا میده (`upgrade`). مثل Windows Update ولی برای سرور. چند دقیقه طول می‌کشه. هر وقت سوال `Y/n` پرسید، Enter بزن؛ اگه پنجره‌ی بنفش/آبی چندصفحه‌ای اومد فقط Enter بزن (گزینه‌ی پیش‌فرض OK).

### ۱.۳ ساخت کاربر غیر-root برای ربات

**چرا نباید با root کار کنیم؟** کاربر root به همه‌ی فایل‌ها و دستورهای سیستم دسترسی کامل داره؛ اگه ربات باگ امنیتی داشته باشه یا اشتباهی یه دستور مخرب بزنی، کل سرور می‌پکه. با یه کاربر معمولی، آسیب فقط محدود به همون کاربر می‌مونه.

```bash
adduser teriaky
```

کاربری به اسم `teriaky` می‌سازه. اول یه رمز ازت می‌خواد (دوبار تایپش کن؛ هنگام تایپ چیزی نمایش نمیده) و بقیه‌ی سوال‌ها (Full Name و…) رو می‌تونی با Enter رد کنی. آخرش `Y` بزن.

```bash
usermod -aG sudo teriaky
```

کاربر `teriaky` رو عضو گروه `sudo` می‌کنه تا موقع نیاز بتونه با پیشوند `sudo` کارهای مدیریتی انجام بده.

حالا با کاربر جدید وارد شو تا بقیه‌ی مراحل رو باهاش انجام بدی:

```bash
su - teriaky
```

این دستور جلوی همین پنجره، کاربرت رو عوض می‌کنه و می‌فرستتت توی پوشه‌ی خونگی کاربر جدید (`/home/teriaky`). خط فرمان باید شبیه `teriaky@ubuntu:~$` بشه.

### ۱.۴ فایروال پایه (ufw) — فقط SSH

**چرا فقط SSH؟** ربات تریاکی با روش **long-polling** کار می‌کنه، یعنی خودش **از داخل سرور به تلگرام وصل میشه** و آپدیت‌ها رو میاره؛ هیچ‌کس از بیرون به ربات وصل نمیشه. پس **هیچ پورت ورودی دیگه‌ای اصلاً لازم نیست باز بشه** و هرچی ببندیم سرور امن‌تره.

```bash
sudo ufw allow OpenSSH
```

به فایروال اجازه میده فقط اتصال‌های SSH (همون راهی که خودت باهاش وصل شدی) عبور کنه. (رمز کاربر `teriaky` رو که ساختی ازت می‌خواد.)

```bash
sudo ufw enable
```

فایروال رو فعال می‌کنه. هشدار میده که ممکنه اتصال SSH فعلی قطع بشه؛ `y` بزن و Enter — نگران نباش، چون مرحله‌ی قبل اجازه‌ی SSH رو دادیم، اتصالت قطع نمیشه.

```bash
sudo ufw status verbose
```

وضعیت فایروال رو نشون میده. باید `Status: active` ببینی و فقط `OpenSSH` با `ALLOW`.

---

## بخش ۲ — نصب پایتون و وابستگی‌ها

### ۲.۱ نصب Python 3.11+ و pip

کد ربات به پایتون **3.10 به بالا** نیاز داره (python-telegram-bot نسخه‌ی 22 به بالا و سینتکس‌های جدید پایتون). Ubuntu 24.04 خودش پایتون 3.12 داره که عالیه:

```bash
sudo apt install -y python3 python3-venv python3-pip git unzip
```

پایتون، ابزار ساخت محیط مجازی (`venv`)، نصب‌کننده‌ی پکیج‌ها (`pip`)، گیت و ابزار بازکردن ZIP رو یک‌جا نصب می‌کنه.

```bash
python3 --version
```

نسخه‌ی پایتون نصب‌شده رو نشون میده. **باید 3.10 به بالا باشه** (مثلاً `Python 3.12.3`).

> ⚠️ **اگه نسخه زیر 3.10 بود** (سرور Ubuntu 22.04 یا قدیمی‌تر): این ریپو رو اضافه کن و پایتون 3.12 رو بگیر:
> ```bash
> sudo apt install -y software-properties-common
> sudo add-apt-repository -y ppa:deadsnakes/ppa
> sudo apt update
> sudo apt install -y python3.12 python3.12-venv
> ```
> و هر جای این راهنما `python3` دیدی، به‌جاش بنویس `python3.12`.

### ۲.۲ دریافت کد روی سرور

دو راه داری؛ **راه اول (آپلود مستقیم ZIP)** برای کسی که با git راحت نیست توصیه میشه و تو بخش ۴ دقیق میگیمش. اگه کدت توی یه ریپوی گیت (GitHub و…) هست:

```bash
git clone https://github.com/USERNAME/REPO.git /home/teriaky/teriaky
```

کد رو از ریپوت دانلود و توی پوشه‌ی `/home/teriaky/teriaky` می‌ریزه. اگه این روش رو رفتی، مرحله‌ی ۴.۱ (آپلود ZIP) رو رد کن.

### ۲.۳ محیط مجازی (venv)

**venv چیه و چرا لازمه؟** یه پوشه‌ی جدا که فقط پکیج‌های پایتونی همین پروژه توش نصب میشن. اگه مستقیم روی پایتون سیستم نصب کنی، اولاً Ubuntu جدید اصلاً جلوت رو می‌گیره و ثانیاً پکیج‌های پروژه‌های مختلف به‌هم می‌ریزن.

```bash
python3 -m venv /home/teriaky/venv
```

یه محیط مجازی توی مسیر `/home/teriaky/venv` می‌سازه. فقط یه‌بار لازمه بسازیش.

```bash
source /home/teriaky/venv/bin/activate
```

محیط مجازی رو **فعال** می‌کنه — از این لحظه هر `python` یا `pip` که بزنی مال venv ـه نه سیستم. اول خط فرمانت `(venv)` اضافه میشه؛ اونو می‌بینی یعنی فعاله.

> ⚠️ **اگه این خطا رو دیدی:** `error: externally-managed-environment` ← یعنی بدون فعال‌کردن venv داری pip می‌زنی. اول دستور `source /home/teriaky/venv/bin/activate` رو بزن بعد ادامه بده. (هر بار که ترمینال رو ببندی و دوباره وصل بشی، باید دوباره activate کنی.)

### ۲.۴ نصب پکیج‌های پروژه

اول مطمئن شو `cd` کردی توی پوشه‌ی پروژه (بعد از مرحله‌ی ۴ این پوشه ساخته میشه — اگه هنوز کد رو نریختی، اول برو بخش ۴.۱ رو انجام بده بعد برگرد اینجا):

```bash
cd /home/teriaky/teriaky
source /home/teriaky/venv/bin/activate
pip install -r requirements.txt
```

به `pip` میگه همه‌ی پکیج‌های لیست‌شده توی `requirements.txt` رو نصب کن (شامل python-telegram-bot، SQLAlchemy، aiosqlite و **asyncpg** برای اتصال به Postgres).

> ⚠️ **اگه موقع نصب asyncpg خطای کامپایل دیدی** (قرمز طولانی با کلمه‌ی `gcc` یا `error`): پیش‌نیازهای بیلد رو نصب کن و دوباره بزن:
> ```bash
> sudo apt install -y libpq-dev gcc python3-dev
> pip install -r requirements.txt
> ```

---

## بخش ۳ — نصب و امن‌سازی PostgreSQL روی همان سرور

### ۳.۱ نصب

```bash
sudo apt install -y postgresql postgresql-contrib
```

سرور دیتابیس PostgreSQL و ابزارهای کمکیش رو نصب می‌کنه. خودکار سرویسش استارت میشه.

```bash
sudo systemctl status postgresql --no-pager
```

وضعیت سرویس Postgres رو نشون میده؛ باید کلمه‌ی `active (exited)` یا `active` رو سبز ببینی. (با زدن `q` از صفحه‌ی وضعیت خارج میشی.)

### ۳.۲ ساخت دیتابیس و کاربر اختصاصی ربات

**چرا کاربر اختصاصی؟** کاربر `postgres` رئیس‌کل دیتابیسه؛ اگه رمزش توی فایل‌های پروژه لو بره کل دیتابیس‌ها میرن. یه کاربر محدود که فقط به یه دیتابیس دسترسی داره، شعاع انفجار لو رفتن رو کوچیک می‌کنه.

```bash
sudo -u postgres psql -c "CREATE USER teriaky WITH PASSWORD 'RAMZ-QAVI-123';"
```

به‌عنوان کاربر ادمین Postgres (`postgres`) یه کاربر دیتابیس جدید به اسم `teriaky` با رمز می‌سازه. **رمز رو عوض کن!**

```bash
sudo -u postgres psql -c "CREATE DATABASE teriaky OWNER teriaky;"
```

دیتابیسی به اسم `teriaky` می‌سازه که مالکش همون کاربر `teriaky` ـه.

### ۳.۳ محدود کردن Postgres به localhost

**نکته‌ی امنیتی مهم:** Postgres ما فقط باید از **خود همین سرور** (ربات و اسکریپت مهاجرت) قابل‌اتصال باشه، نه از اینترنت. خوشبختانه نصب پیش‌فرض Ubuntu دقیقاً همینه — ولی **حتماً چکش کن**:

```bash
ls /etc/postgresql/
```

نسخه‌ی Postgres نصب‌شده رو نشون میده (مثلاً `16`). همین عدد رو تو دستورهای بعدی استفاده کن.

```bash
sudo grep -n "listen_addresses" /etc/postgresql/16/main/postgresql.conf
```

توی فایل `postgresql.conf` دنبال خط `listen_addresses` می‌گرده. باید دقیقاً این باشه:

```
listen_addresses = 'localhost'
```

یعنی Postgres فقط روی آدرس داخلی خود سرور گوش میده. **اگه `'localhost'` نبود یا اول خط `#` داشت و مقدارش ستاره بود (`*`):** با `sudo nano /etc/postgresql/16/main/postgresql.conf` بازش کن، خط رو دقیقاً `listen_addresses = 'localhost'` کن (بدون # اولش)، با `Ctrl+O` و Enter ذخیره و با `Ctrl+X` خارج شو، بعد سرویس رو ری‌استارت کن: `sudo systemctl restart postgresql`

```bash
sudo grep -Ev "^\s*#|^\s*$" /etc/postgresql/16/main/pg_hba.conf
```

خط‌های فعال (غیر کامنت) فایل `pg_hba.conf` رو نشون میده. این فایل میگه «کی از کجا اجازه‌ی اتصال داره». خط‌های فعالش **فقط** باید این‌جور چیزا باشن:

```
local   all   postgres                  peer
local   all   all                       peer
host    all   all   127.0.0.1/32        scram-sha-256
host    all   all   ::1/128             scram-sha-256
```

یعنی: اتصال فقط یا از سوکت داخلی (`local`) یا از IP خود سرور (`127.0.0.1` و `::1`). **اگه خطی با `0.0.0.0/0` دیدی، همون خط رو با nano پیدا کن و اولش `#` بذار** (کامنتش کن) و `sudo systemctl restart postgresql` رو بزن. این یعنی دیتابیست از بیرون سرور کاملاً نایابه.

### ۳.۴ تست اتصال قبل از ادامه

```bash
PGPASSWORD='RAMZ-QAVI-123' psql -h 127.0.0.1 -U teriaky -d teriaky -c "SELECT 1;"
```

با کاربر و رمز جدید به دیتابیس وصل میشه و یه کوئری ساده می‌زنه. خروجی باید یه جدول کوچیک با عدد `1` باشه، مثل:

```
 ?column?
----------
        1
(1 row)
```

> ⚠️ **اگه خطای `password authentication failed` دیدی:** رمز توی دستور با رمزی که موقع ساخت کاربر زدی فرق داره (حروف بزرگ/کوچیک مهم‌ان). دوباره ۳.۲ رو با رمز درست انجام بده.
> ⚠️ **اگه خطای `database "teriaky" does not exist` دیدی:** مرحله‌ی ۳.۲ دوم انجام نشده، برگرد انجامش بده.

---

## بخش ۴ — انتقال فایل‌های پروژه و بک‌آپ SQLite قدیمی

### ۴.۱ آپلود ZIP پروژه از ویندوز با scp

برگرد به **PowerShell ویندوزت** (نه سرور). فرض می‌کنیم فایل ZIP توی Downloads ـه:

```powershell
scp "C:\Users\YourName\Downloads\teriaky.zip" teriaky@SERVER_IP:/home/teriaky/
```

`scp` فایل رو رمزنگاری‌شده از ویندوزت به پوشه‌ی خونگی کاربر `teriaky` روی سرور کپی می‌کنه. مسیر فایل و `SERVER_IP` رو با مال خودت عوض کن و رمز کاربر `teriaky` (که تو ۱.۳ ساختی) رو بزن.

حالا توی پنجره‌ی SSH سرور، فایل رو باز کن:

```bash
cd /home/teriaky
unzip -o teriaky.zip -d teriaky
ls /home/teriaky/teriaky
```

فایل ZIP رو توی پوشه‌ی `teriaky` باز می‌کنه و محتواش رو لیست می‌کنه — باید `bot.py` و `config.py` و بقیه فایل‌ها رو ببینی.

### ۴.۲ گرفتن بک‌آپ SQLite از ربات ریلوی و آپلودش

توی تلگرام به ربات خودت (همونی که هنوز روی Railway روشنه) با اکانت ادمین برو و دستور **`/backup`** رو بزن — ربات فایل دیتابیس فعلی رو برات می‌فرسته. فایل رو دانلود کن رو ویندوزت.

حالا با PowerShell ویندوز آپلودش کن (اسم فایل با مال خودت):

```powershell
scp "C:\Users\YourName\Downloads\teriaky-backup-2026-08-04.db" teriaky@SERVER_IP:/home/teriaky/
```

می‌فرستتش توی `/home/teriaky/` روی سرور. مسیر کاملش میشه مثلاً `/home/teriaky/teriaky-backup-2026-08-04.db` — همین مسیر رو تو بخش ۵ استفاده می‌کنیم.

---

## بخش ۵ — مهاجرت SQLite به PostgreSQL (لوکال، روی همین سرور)

برخلاف مهاجرت بین دو سرویس جدا، این‌بار فایل SQLite و Postgres **هر دو روی یه سرورن** و اسکریپت محلی اجرا میشه.

**ترتیب پیشنهادی برای کمترین داون‌تایم:** چون اسکریپت مهاجرت **idempotent** ـه (ردیف تکراری نمی‌سازه)، می‌تونی اول با بک‌آپ فعلی همه‌چی رو مهاجرت و تست کنی؛ بعد موقع سوییچ نهایی: ربات ریلوی رو خاموش کن ← یه `/backup` تازه بگیر ← آپلود ← همون اسکریپت رو دوباره بزن (فقط ردیف‌های جدید کپی میشه) ← ربات VPS رو روشن کن.

### ۵.۱ تنظیم متغیرهای مهاجرت

توی SSH سرور (با کاربر `teriaky` و venv فعال):

```bash
cd /home/teriaky/teriaky
source /home/teriaky/venv/bin/activate
export TERIAKY_MIGRATE_FROM=/home/teriaky/teriaky-backup-2026-08-04.db
export TERIAKY_MIGRATE_TO='postgresql+asyncpg://teriaky:RAMZ-QAVI-123@localhost:5432/teriaky'
```

دوتا متغیر فقط برای همین پنجره‌ی فعلی ست میشن: `TERIAKY_MIGRATE_FROM` مسیر فایل SQLite آپلودشده و `TERIAKY_MIGRATE_TO` آدرس کامل Postgres محلی. اسم فایل و رمز رو با مال خودت عوض کن. فرمت آدرس دقیقاً باید این شکلی باشه: `postgresql+asyncpg://USER:PASSWORD@localhost:5432/DATABASE`

> ⚠️ **راه‌فرار (درست کار می‌کنه ولی پیشنهاد نمیشه):** اگه `postgresql://` خام هم بنویسی، خود کد (`config._normalize_db_url` و خود اسکریپت) به‌خودی به asyncpg تبدیلش می‌کنه — ولی بهتره از اول کامل بنویسی.

### ۵.۲ اجرای مهاجرت

```bash
python migrate_to_postgres.py --yes
```

اسکریپت همه‌ی جدول‌ها رو با ترتیب وابستگی کپی می‌کنه. فلگ `--yes` تایید تعاملی رو رد می‌کنه. **اگه بک‌آپت قدیمیه و بعضی جدول‌های جدید کد رو نداره، هرکدوم با یه خط «⏭ وجود نداره (احتمالاً بک‌آپ قدیمیه)، رد شد» کنار گذاشته میشن (نه کرش) و بقیه جدول‌ها عادی کپی و verify میشن (راند ۳۲).** خروجی موفق باید این شکلی باشه:

> 💡 **راند ۳۵:** اگه بعد مهاجرت/ری‌استور دیتابیست ستون‌های جدید مدل رو نداشته باشه (مثلاً بک‌آپت خیلی قدیمیه)، موقع بالا اومدن ربات `_sync_model_columns` خودش همه‌ی ستون‌های گمشده رو با مقادیر پیش‌فرض امن ALTER می‌کنه و دیگه کرش‌لوپ `OperationalError: no such column` نمی‌گیری؛ جزئیاتش تو لاگ systemd قابل دیدنه (`journalctl -u teriaky -f`).

```
📦 users: 148 ردیف جدید، 0 ردیف تکراری رد شد
📦 dogs: 37 ردیف جدید، 0 ردیف تکراری رد شد
...
🔧 سریال‌های Postgres با بیشترین id هر جدول سینک شدن
🎉 مهاجرت تموم شد — 531 ردیف جدید کپی شد و verify برقراره
```

اگه دوباره همون دستور رو بزنی، باید `0 ردیف جدید` ببینی — یعنی هیچی تکراری نشده.

> 💡 **راه فرار بدون ترمینال (راند ۳۴):** اگه ربات جدیدت از قبل روی Postgres بالاست و فقط می‌خوای دیتای یه بک‌آپ SQLite قدیمی رو بریزی روش، هم می‌تونی این اسکریپت رو بزنی هم می‌تونی راحت‌ترش کنی: تو تلگرام `/upload_backup` بزن و همون فایل .db رو بفرست — ربات خودش تشخیص میده و دیتاش رو با دیتابیس زنده ادغام می‌کنه (تکراری‌ها رد میشن). فرقش با این اسکریپت: اسکریپت وقتی بده که بک‌آپ خیلی گنده‌ست یا ربات خاموشه.

> ⚠️ **اگه خطای `connection refused` دیدی:** سرویس Postgres بالا نیست؛ بزن `sudo systemctl start postgresql` و دوباره.
> ⚠️ **اگه خطای `password authentication failed` دیدی:** رمز توی `TERIAKY_MIGRATE_TO` با رمز کاربر دیتابیس یکی نیست؛ مرحله‌ی ۵.۱ رو درست ست کن.
> ⚠️ **اگه خطای `ModuleNotFoundError: No module named 'asyncpg'` دیدی:** venv فعال نیس؛ `source /home/teriaky/venv/bin/activate` بزن و دوباره.

### ۵.۳ اطمینان مضاعف با verify-only

```bash
python migrate_to_postgres.py --verify-only
```

بدون هیچ نوشتنی، **تعداد ردیف تک‌تک جدول‌ها** رو بین SQLite و Postgres مقایسه می‌کنه و جدول چاپ می‌کنه. آخرش باید ببینی:

```
🎉 verify برقراره، تعداد ردیف همه جدول‌ها یکیه
```

اگه `❌ verify شکست خورد` دیدی، یعنی جدولی جا مونده؛ خط‌های `⚠️` رو پیدا کن و مهاجرت (۵.۲) رو دوباره بزن چون idempotent ـه فقط کمبودها رو پر می‌کنه.

---

## بخش ۶ — اجرای دائمی ربات با systemd

### ۶.۱ systemd چیه و چرا لازمه؟

`systemd` مدیر سرویس‌های خود اوبونتوئه. اگه ربات رو دستی اجرا کنی (`python bot.py`)، با بستن پنجره‌ی SSH یا ری‌استارت سرور یا قطعی برق، ربات **می‌میره و برنمی‌گرده**. با تعریف یه «سرویس» برای systemd: ربات پس‌زمینه اجرا میشه، **کرش کرد خودکار ری‌استارت میشه** (مثل `restartPolicyType: ON_FAILURE` که توی railway.json داشتی) و **بعد از روشن‌شدن سرور خودش بالا میاد**.

### ۶.۲ تنظیمات: فقط یه فایل .env (راند ۱۸)

**دیگه خبری از export دستی و فایل دوم نیس.** خود زیپ یه فایل `.env` داره که کنار پروژه می‌شینه و ربات موقع استارت **خودش خودکار** می‌خونه‌ش. پس فقط همین یه فایل رو پر کن:

```bash
nano /home/teriaky/teriaky/.env
```

مقادیر خودت رو بذار (توکن، آیدی عددی ادمین‌ها و اگه Postgres داری خط `TERIAKY_DB` رو فعال کن). ذخیره: `Ctrl+O` ← Enter ← خروج: `Ctrl+X`.

**یا با دستور دائمی (مثل export ولی موندگار):**

```bash
cd /home/teriaky/teriaky
python3 teriaky_set.py TERIAKY_TOKEN 1234567890:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
python3 teriaky_set.py TERIAKY_ADMIN_IDS 1001,1003
python3 teriaky_set.py --list   # وضعیت فعلی رو با ماسک توکن نشون میده
```

هر دو روش یه کار می‌کنن: نوشتن توی همون یه فایل `.env`. بعدش یه ری‌استارت کافیه (`sudo systemctl restart teriaky`) و مقادیر **بعد از ریبوت سرور هم می‌مونن**. ربات موقع بوت تو لاگ اعلام می‌کنه چندتا متغیر از فایل خونده؛ اگه دیدی نوشته «فایل .env لود نشد» یعنی یا فایل پیدا نشده یا همه‌ی مقادیر از قبل تو محیط سیستم ست‌شده (محیط همیشه اولویت داره).

> مهاجرت از حالت قبلی: اگه `/home/teriaky/teriaky.env` داری و توی سرویس خط `EnvironmentFile=` گذاشتی، اون خط رو از فایل سرویس پاک کن و مقادیر رو منتقل کن به `.env` پروژه:
>
> ```bash
> cat /home/teriaky/teriaky.env >> /home/teriaky/teriaky/.env
> sudo systemctl daemon-reload && sudo systemctl restart teriaky
> ```
>
> از این به بعد تنها منبع تنظیمات همون یه فایله، پس حالت «تغییر دادم ولی اعمال نشد» دیگه پیش نمیاد.

### ۶.۳ فایل سرویس systemd

```bash
sudo nano /etc/systemd/system/teriaky.service
```

این متن رو دقیقاً بریز توش:

```ini
[Unit]
Description=Teriaky Telegram Bot
After=network-online.target postgresql.service
Wants=network-online.target

[Service]
Type=simple
User=teriaky
WorkingDirectory=/home/teriaky/teriaky
ExecStart=/home/teriaky/venv/bin/python bot.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

تکه‌تکه‌ش برای ای که بدونی چیکار می‌کنه:
- `After=... postgresql.service` — اول Postgres بالا بیاد بعد ربات (ربات بدون دیتابیس نمی‌تونه استارت بشه)
- `User=teriaky` — با کاربر غیر-root اجرا میشه
- `WorkingDirectory` — همون پوشه‌ی پروژه (معادل کاری که Procfile توی Railway می‌کرد)
- تنظیمات: نه `EnvironmentFile` لازمه نه `export` دستی — خود ربات موقع استارت فایل `.env` کنار پروژه رو خودکار می‌خونه و تو لاگ می‌گه چی خونده؛ برای تغییر هر مقدار هم `python3 teriaky_set.py KEY VALUE` رو تو پوشه‌ی پروژه بزن و سرویس رو ری‌استارت کن
- `ExecStart` — از پایتون **داخل venv** استفاده می‌کنه (نه پایتون سیستم) تا پکیج‌های ربات پیدا بشن؛ اجرا مستقیم `bot.py`، همون long-polling همیشگی، **بدون نیاز به پورت باز**
- `Restart=on-failure` + `RestartSec=5` — کرش کرد ۵ ثانیه بعد خودش بالا میاد

ذخیره و خروج با `Ctrl+O` ← Enter ← `Ctrl+X`.

### ۶.۴ فعال‌سازی و استارت

```bash
sudo systemctl daemon-reload
```

به systemd میگه فایل‌های سرویس جدید رو بخونه (هر بار یه سرویس رو عوض کردی این لازمه).

```bash
sudo systemctl enable --now teriaky
```

هم سرویس رو **فعال** می‌کنه برای بالا اومدن با بوت سرور (`enable`)، هم **همین الان** استارتش می‌کنه (`--now`).

```bash
sudo systemctl status teriaky --no-pager
```

وضعیت سرویس رو نشون میده. خط سبز `active (running)` یعنی ربات بالاست. (`q` برای خروج.)

> ⚠️ **نکته‌ی حیاتی — تداخل دو ربات:** قبل از `enable --now` باید ربات Railway رو **کاملاً خاموش** کرده باشی (متغیر توکنش رو پاک کن یا سرویسش رو Stop کن). تلگرام اجازه نمی‌ده دو نفر هم‌زمان با یه توکن polling بگیرن و تو لاگ خطای `Conflict: terminated by other getUpdates request` می‌بینی.
> ⚠️ **اگه وضعیت `failed` شد:** معمولاً یکی از ایناست — آدرس اشتباه تو ExecStart (پوشه‌ی پروژه واقعاً `/home/teriaky/teriaky` ـه؟)، فرمت بد `.env` (کوتیشن/fاصله)، یا رمز دیتابیس اشتباه. علت دقیق رو با دستور لاگ پایین ببین.

### ۶.۵ دیدن لاگ زنده (معادل تب Logs توی Railway)

```bash
journalctl -u teriaky -f
```

لاگ‌های سرویس رو **زنده** دنبال می‌کنه (دقیقاً معادل تب Logs توی Railway). توقف دنبال‌کردن با `Ctrl+C` (سرویس خاموش نمیشه، فقط از نمایش لاگ خارج میشی). برای دیدن ۱۰۰ خط آخر بدون دنبال‌کردن:

```bash
journalctl -u teriaky -n 100 --no-pager
```

---

## بخش ۷ — بک‌آپ دوره‌ای خودکار

### ۷.۱ اسکریپت بک‌آپ روزانه از Postgres

```bash
mkdir -p /home/teriaky/backups
nano /home/teriaky/backup_teriaky.sh
```

محتوای اسکریپت (رمز رو با مال خودت عوض کن):

```bash
#!/usr/bin/env bash
set -euo pipefail
TS=$(date +%Y%m%d-%H%M%S)
OUT_DIR=/home/teriaky/backups
pg_dump "postgresql://teriaky:RAMZ-QAVI-123@localhost:5432/teriaky" | gzip > "$OUT_DIR/teriaky-$TS.sql.gz"
find "$OUT_DIR" -name 'teriaky-*.sql.gz' -mtime +14 -delete
```

تکه‌تکه‌ش: نام فایل با تاریخ-ساعت دقیق می‌سازه (`20260804-033001`)، `pg_dump` کل دیتابیس رو به SQL تبدیل و فشرده (gzip) تو پوشه‌ی `backups` ذخیره می‌کنه، و خط آخر بک‌آپ‌های قدیمی‌تر از ۱۴ روز رو پاک می‌کنه که هارد پر نشه.

> 💡 **راه دیگه (راند ۳۳):** رو VPS لازم نیس برای بک‌آپ دستی ترمینال باز کنی؛ `/backup` تو ربات همون دامپ فشرده pg_dump (‎.sql.gz) رو می‌فرسته و `/upload_backup` هم همون فایل‌ها رو ری‌استور می‌کنه — **راند ۳۴:** فایل SQLite قدیمی (.db) روی دیتابیس Postgres دیگه رد نمیشه؛ ربات خودش دیتاش رو تو پس‌زمینه با دیتابیس فعلی **ادغام** می‌کنه (بک‌آپ احتیاطی تو `/tmp/teriaky-preimport-*.sql`، ردیف تکراری/یتیم رد میشه و خلاصه‌اش تو تلگرام میاد) و بدون SSH انجامش میدی — سرویس بک‌آپ قبل از ری‌لود، خودکار یه بک‌آپ احتیاطی از وضعیت فعلی می‌گیره و وسط خطا برمی‌گرده سر جاش. پیش‌شرطش هم فقط اینه که `psql` و `pg_dump` نصب باشن (روی این سرور که هستن).

```bash
chmod +x /home/teriaky/backup_teriaky.sh
chmod 700 /home/teriaky/backup_teriaky.sh
bash /home/teriaky/backup_teriaky.sh
ls -lh /home/teriaky/backups/
```

اول اسکریپت رو اجراپذیر و محرمانه می‌کنه، بعد **یه‌بار دستی اجراش می‌کنه برای تست** و آخرش فایل ساخته‌شده رو نشونت میده. باید یه فایل `teriaky-XXXX.sql.gz` با حجم بیشتر از چند کیلوبایت ببینی.

> نکته: خود ربات هم بک‌آپ داخلی داره — اگه `TERIAKY_DAILY_BACKUP_CHAT_ID` (آیدی گروه ادمین) رو ست کنی، هر روز ساعت ۴ بامداد به‌وقت ایران فایل دیتابیس رو (روی Postgres خروجی فشرده pg_dump) مستقیم توی تلگرام به همون گروه می‌فرسته. دوتا بک‌آپ مستقل از هم بهتر از یکی‌ست.

### ۷.۲ زمان‌بندی خودکار با cron

```bash
crontab -e
```

لیست کارهای زمان‌بندی‌شده‌ی کاربرت رو باز می‌کنه (بار اول ویرایشگر می‌پرسه، `nano` رو انتخاب کن یعنی گزینه‌ی ۱). این خط رو ته فایل اضافه کن:

```cron
30 3 * * * /home/teriaky/backup_teriaky.sh >> /home/teriaky/backups/backup.log 2>&1
```

یعنی «هر شب ساعت ۳:۳۰ بامداد به وقت سرور، اسکریپت بک‌آپ رو بزن و خروجی‌ش رو تو لاگ بنویس». ذخیره و خارج شو.

### ۷.۳ چرا بک‌آپ باید بره بیرون از سرور؟

اگه خود سرور کامل بسوزه (حذف تصادفی VPS، خرابی دیتاسنتر) بک‌آپ‌های داخلش هم باهاش میرن! هر چند هفته یه‌بار با scp از ویندوزت دانلودشون کن:

```powershell
scp "teriaky@SERVER_IP:/home/teriaky/backups/teriaky-20260804-033001.sql.gz" "C:\Users\YourName\Downloads\"
```

یه کپی روی ویندوزت (یا درایو ابری) همیشه داشته باش.

---

## بخش ۸ — چک‌لیست نهایی تأیید سلامت ✅

بعد از طی همه‌ی مراحل، به ترتیب چک کن:

1. **ربات جواب میده** — توی تلگرام به ربات پی‌وی بزن `/start` و ببین منوی اصلی میاد.
2. **دیتای مهاجرت‌شده هست** — به قسمت پروفایل (یا هر دارایی قدیمی‌ات مثل سگ/زمین/بانک) نگاه کن؛ چیزی که روز آخر روی Railway داشتی باید دقیقاً همون باشه.
3. **کاربر واقعی از دیتابیس مهاجرت‌شده چک بشه** — از یه نفر دیگه (یه دوست که قبلاً بازی می‌کرد) بخواه `/start` بزنه و ببینه پروفایل و دارایی‌اش سر جاشه.
4. **لاگ تمیزه** — `journalctl -u teriaky -n 100 --no-pager` نباید خطای قرمز پشت‌سرهم داشته باشه (یه `Connection refused` گاهی‌به‌گاهی تلگرام طبیعیه ولی `Conflict` نه — اون یعنی ربات ریلوی هنوز روشنه).
5. **سرویس برای بوت فعاله** — `systemctl is-enabled teriaky` باید چاپ کنه `enabled`.
6. **تست زنده‌ماندن بعد از ری‌استارت** — `sudo reboot` بزن، ۲-۳ دقیقه صبر کن، **بدون اینکه به سرور SSH بزنی**، توی تلگرام به ربات `/start` بده؛ اگه جواب داد یعنی systemd خودش همه‌چی رو بالا آورده.
7. **بک‌آپ دستی کار می‌کنه** — `ls -lh /home/teriaky/backups/` فایل `.sql.gz` تازه داره (و فرداشب cron خودش یکی دیگه می‌سازه).
8. **فایروال فقط SSH ـه** — `sudo ufw status` فقط `OpenSSH` با `ALLOW` داره.
9. **Railway تمیزه** — سرویس قدیمی ریلوی خاموشه (بعد از چند روز اطمینان می‌تونی کامل حذفش کنی).

🎉 تموم! ربات تریاکی حالا روی سرور خودته، با Postgres محلی، استارتاپ خودکار، بک‌آپ شبانه و بدون هزینه‌ی سرویس ابری.
